/**
 * Trieve API
 * Trieve OpenAPI Specification. This document describes all of the operations available through the Trieve API.
 *
 * The version of the OpenAPI document: 0.5.0
 * Contact: developers@trieve.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ScoreChunkDTO from './ScoreChunkDTO';

/**
 * The GroupScoreChunkDTO model module.
 * @module model/GroupScoreChunkDTO
 * @version 0.5.0
 */
class GroupScoreChunkDTO {
    /**
     * Constructs a new <code>GroupScoreChunkDTO</code>.
     * @alias module:model/GroupScoreChunkDTO
     * @param groupId {String} 
     * @param metadata {Array.<module:model/ScoreChunkDTO>} 
     */
    constructor(groupId, metadata) { 
        
        GroupScoreChunkDTO.initialize(this, groupId, metadata);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, groupId, metadata) { 
        obj['group_id'] = groupId;
        obj['metadata'] = metadata;
    }

    /**
     * Constructs a <code>GroupScoreChunkDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GroupScoreChunkDTO} obj Optional instance to populate.
     * @return {module:model/GroupScoreChunkDTO} The populated <code>GroupScoreChunkDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GroupScoreChunkDTO();

            if (data.hasOwnProperty('group_id')) {
                obj['group_id'] = ApiClient.convertToType(data['group_id'], 'String');
            }
            if (data.hasOwnProperty('group_tracking_id')) {
                obj['group_tracking_id'] = ApiClient.convertToType(data['group_tracking_id'], 'String');
            }
            if (data.hasOwnProperty('metadata')) {
                obj['metadata'] = ApiClient.convertToType(data['metadata'], [ScoreChunkDTO]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GroupScoreChunkDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GroupScoreChunkDTO</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GroupScoreChunkDTO.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['group_id'] && !(typeof data['group_id'] === 'string' || data['group_id'] instanceof String)) {
            throw new Error("Expected the field `group_id` to be a primitive type in the JSON string but got " + data['group_id']);
        }
        // ensure the json data is a string
        if (data['group_tracking_id'] && !(typeof data['group_tracking_id'] === 'string' || data['group_tracking_id'] instanceof String)) {
            throw new Error("Expected the field `group_tracking_id` to be a primitive type in the JSON string but got " + data['group_tracking_id']);
        }
        if (data['metadata']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['metadata'])) {
                throw new Error("Expected the field `metadata` to be an array in the JSON data but got " + data['metadata']);
            }
            // validate the optional field `metadata` (array)
            for (const item of data['metadata']) {
                ScoreChunkDTO.validateJSON(item);
            };
        }

        return true;
    }


}

GroupScoreChunkDTO.RequiredProperties = ["group_id", "metadata"];

/**
 * @member {String} group_id
 */
GroupScoreChunkDTO.prototype['group_id'] = undefined;

/**
 * @member {String} group_tracking_id
 */
GroupScoreChunkDTO.prototype['group_tracking_id'] = undefined;

/**
 * @member {Array.<module:model/ScoreChunkDTO>} metadata
 */
GroupScoreChunkDTO.prototype['metadata'] = undefined;






export default GroupScoreChunkDTO;

