/**
 * Trieve API
 * Trieve OpenAPI Specification. This document describes all of the operations available through the Trieve API.
 *
 * The version of the OpenAPI document: 0.5.0
 * Contact: developers@trieve.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The StripePlan model module.
 * @module model/StripePlan
 * @version 0.5.0
 */
class StripePlan {
    /**
     * Constructs a new <code>StripePlan</code>.
     * @alias module:model/StripePlan
     * @param amount {Number} 
     * @param chunkCount {Number} 
     * @param createdAt {Date} 
     * @param datasetCount {Number} 
     * @param fileStorage {Number} 
     * @param id {String} 
     * @param messageCount {Number} 
     * @param name {String} 
     * @param stripeId {String} 
     * @param updatedAt {Date} 
     * @param userCount {Number} 
     */
    constructor(amount, chunkCount, createdAt, datasetCount, fileStorage, id, messageCount, name, stripeId, updatedAt, userCount) { 
        
        StripePlan.initialize(this, amount, chunkCount, createdAt, datasetCount, fileStorage, id, messageCount, name, stripeId, updatedAt, userCount);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amount, chunkCount, createdAt, datasetCount, fileStorage, id, messageCount, name, stripeId, updatedAt, userCount) { 
        obj['amount'] = amount;
        obj['chunk_count'] = chunkCount;
        obj['created_at'] = createdAt;
        obj['dataset_count'] = datasetCount;
        obj['file_storage'] = fileStorage;
        obj['id'] = id;
        obj['message_count'] = messageCount;
        obj['name'] = name;
        obj['stripe_id'] = stripeId;
        obj['updated_at'] = updatedAt;
        obj['user_count'] = userCount;
    }

    /**
     * Constructs a <code>StripePlan</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/StripePlan} obj Optional instance to populate.
     * @return {module:model/StripePlan} The populated <code>StripePlan</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new StripePlan();

            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('chunk_count')) {
                obj['chunk_count'] = ApiClient.convertToType(data['chunk_count'], 'Number');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('dataset_count')) {
                obj['dataset_count'] = ApiClient.convertToType(data['dataset_count'], 'Number');
            }
            if (data.hasOwnProperty('file_storage')) {
                obj['file_storage'] = ApiClient.convertToType(data['file_storage'], 'Number');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('message_count')) {
                obj['message_count'] = ApiClient.convertToType(data['message_count'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('stripe_id')) {
                obj['stripe_id'] = ApiClient.convertToType(data['stripe_id'], 'String');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('user_count')) {
                obj['user_count'] = ApiClient.convertToType(data['user_count'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>StripePlan</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>StripePlan</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of StripePlan.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['stripe_id'] && !(typeof data['stripe_id'] === 'string' || data['stripe_id'] instanceof String)) {
            throw new Error("Expected the field `stripe_id` to be a primitive type in the JSON string but got " + data['stripe_id']);
        }

        return true;
    }


}

StripePlan.RequiredProperties = ["amount", "chunk_count", "created_at", "dataset_count", "file_storage", "id", "message_count", "name", "stripe_id", "updated_at", "user_count"];

/**
 * @member {Number} amount
 */
StripePlan.prototype['amount'] = undefined;

/**
 * @member {Number} chunk_count
 */
StripePlan.prototype['chunk_count'] = undefined;

/**
 * @member {Date} created_at
 */
StripePlan.prototype['created_at'] = undefined;

/**
 * @member {Number} dataset_count
 */
StripePlan.prototype['dataset_count'] = undefined;

/**
 * @member {Number} file_storage
 */
StripePlan.prototype['file_storage'] = undefined;

/**
 * @member {String} id
 */
StripePlan.prototype['id'] = undefined;

/**
 * @member {Number} message_count
 */
StripePlan.prototype['message_count'] = undefined;

/**
 * @member {String} name
 */
StripePlan.prototype['name'] = undefined;

/**
 * @member {String} stripe_id
 */
StripePlan.prototype['stripe_id'] = undefined;

/**
 * @member {Date} updated_at
 */
StripePlan.prototype['updated_at'] = undefined;

/**
 * @member {Number} user_count
 */
StripePlan.prototype['user_count'] = undefined;






export default StripePlan;

