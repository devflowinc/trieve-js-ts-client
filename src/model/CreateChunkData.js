/**
 * Trieve API
 * Trieve OpenAPI Specification. This document describes all of the operations available through the Trieve API.
 *
 * The version of the OpenAPI document: 0.5.0
 * Contact: developers@trieve.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ChunkData from './ChunkData';
import CreateBatchChunkData from './CreateBatchChunkData';

/**
 * The CreateChunkData model module.
 * @module model/CreateChunkData
 * @version 0.5.0
 */
class CreateChunkData {
    /**
     * Constructs a new <code>CreateChunkData</code>.
     * @alias module:model/CreateChunkData
     * @param {(module:model/ChunkData|module:model/CreateBatchChunkData)} instance The actual instance to initialize CreateChunkData.
     */
    constructor(instance = null) {
        if (instance === null) {
            this.actualInstance = null;
            return;
        }
        var match = 0;
        var errorMessages = [];
        try {
            if (typeof instance === "ChunkData") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                ChunkData.validateJSON(instance); // throw an exception if no match
                // create ChunkData from JS object
                this.actualInstance = ChunkData.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into ChunkData
            errorMessages.push("Failed to construct ChunkData: " + err)
        }

        try {
            if (typeof instance === "CreateBatchChunkData") {
                this.actualInstance = instance;
            } else {
                // plain JS object
                // validate the object
                CreateBatchChunkData.validateJSON(instance); // throw an exception if no match
                // create CreateBatchChunkData from JS object
                this.actualInstance = CreateBatchChunkData.constructFromObject(instance);
            }
            match++;
        } catch(err) {
            // json data failed to deserialize into CreateBatchChunkData
            errorMessages.push("Failed to construct CreateBatchChunkData: " + err)
        }

        if (match > 1) {
            throw new Error("Multiple matches found constructing `CreateChunkData` with oneOf schemas ChunkData, CreateBatchChunkData. Input: " + JSON.stringify(instance));
        } else if (match === 0) {
            this.actualInstance = null; // clear the actual instance in case there are multiple matches
            throw new Error("No match found constructing `CreateChunkData` with oneOf schemas ChunkData, CreateBatchChunkData. Details: " +
                            errorMessages.join(", "));
        } else { // only 1 match
            // the input is valid
        }
    }

    /**
     * Constructs a <code>CreateChunkData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateChunkData} obj Optional instance to populate.
     * @return {module:model/CreateChunkData} The populated <code>CreateChunkData</code> instance.
     */
    static constructFromObject(data, obj) {
        return new CreateChunkData(data);
    }

    /**
     * Gets the actual instance, which can be <code>ChunkData</code>, <code>CreateBatchChunkData</code>.
     * @return {(module:model/ChunkData|module:model/CreateBatchChunkData)} The actual instance.
     */
    getActualInstance() {
        return this.actualInstance;
    }

    /**
     * Sets the actual instance, which can be <code>ChunkData</code>, <code>CreateBatchChunkData</code>.
     * @param {(module:model/ChunkData|module:model/CreateBatchChunkData)} obj The actual instance.
     */
    setActualInstance(obj) {
       this.actualInstance = CreateChunkData.constructFromObject(obj).getActualInstance();
    }

    /**
     * Returns the JSON representation of the actual instance.
     * @return {string}
     */
    toJSON = function(){
        return this.getActualInstance();
    }

    /**
     * Create an instance of CreateChunkData from a JSON string.
     * @param {string} json_string JSON string.
     * @return {module:model/CreateChunkData} An instance of CreateChunkData.
     */
    static fromJSON = function(json_string){
        return CreateChunkData.constructFromObject(JSON.parse(json_string));
    }
}

/**
 * HTML content of the chunk. This can also be plaintext. The innerText of the HTML will be used to create the embedding vector. The point of using HTML is for convienience, as some users have applications where users submit HTML content.
 * @member {String} chunk_html
 */
CreateChunkData.prototype['chunk_html'] = undefined;

/**
 * Chunk_vector is a vector of floats which can be used instead of generating a new embedding. This is useful for when you are using a pre-embedded dataset. If this is not provided, the innerText of the chunk_html will be used to create the embedding.
 * @member {Array.<Number>} chunk_vector
 */
CreateChunkData.prototype['chunk_vector'] = undefined;

/**
 * File_uuid is the uuid of the file that the chunk is associated with. This is used to associate chunks with files. This is useful for when you want to delete a file and all of its associated chunks.
 * @member {String} file_id
 */
CreateChunkData.prototype['file_id'] = undefined;

/**
 * Group ids are the ids of the groups that the chunk should be placed into. This is useful for when you want to create a chunk and add it to a group or multiple groups in one request. Necessary because this route queues the chunk for ingestion and the chunk may not exist yet immediately after response.
 * @member {Array.<String>} group_ids
 */
CreateChunkData.prototype['group_ids'] = undefined;

/**
 * Group tracking_ids are the tracking_ids of the groups that the chunk should be placed into. This is useful for when you want to create a chunk and add it to a group or multiple groups in one request. Necessary because this route queues the chunk for ingestion and the chunk may not exist yet immediately after response.
 * @member {Array.<String>} group_tracking_ids
 */
CreateChunkData.prototype['group_tracking_ids'] = undefined;

/**
 * Link to the chunk. This can also be any string. Frequently, this is a link to the source of the chunk. The link value will not affect the embedding creation.
 * @member {String} link
 */
CreateChunkData.prototype['link'] = undefined;

/**
 * Metadata is a JSON object which can be used to filter chunks. This is useful for when you want to filter chunks by arbitrary metadata. Unlike with tag filtering, there is a performance hit for filtering on metadata.
 * @member {Object} metadata
 */
CreateChunkData.prototype['metadata'] = undefined;

/**
 * Split avg is a boolean which tells the server to split the text in the chunk_html into smaller chunks and average their resulting vectors. This is useful for when you want to create a chunk from a large piece of text and want to split it into smaller chunks to create a more fuzzy average dense vector. The sparse vector will be generated normally with no averaging. By default this is false.
 * @member {Boolean} split_avg
 */
CreateChunkData.prototype['split_avg'] = undefined;

/**
 * Tag set is a list of tags. This can be used to filter chunks by tag. Unlike with metadata filtering, HNSW indices will exist for each tag such that there is not a performance hit for filtering on them.
 * @member {Array.<String>} tag_set
 */
CreateChunkData.prototype['tag_set'] = undefined;

/**
 * Time_stamp should be an ISO 8601 combined date and time without timezone. It is used for time window filtering and recency-biasing search results.
 * @member {String} time_stamp
 */
CreateChunkData.prototype['time_stamp'] = undefined;

/**
 * Tracking_id is a string which can be used to identify a chunk. This is useful for when you are coordinating with an external system and want to use the tracking_id to identify the chunk.
 * @member {String} tracking_id
 */
CreateChunkData.prototype['tracking_id'] = undefined;

/**
 * Upsert when a chunk with the same tracking_id exists. By default this is false, and the request will fail if a chunk with the same tracking_id exists. If this is true, the chunk will be updated if a chunk with the same tracking_id exists.
 * @member {Boolean} upsert_by_tracking_id
 */
CreateChunkData.prototype['upsert_by_tracking_id'] = undefined;

/**
 * Weight is a float which can be used to bias search results. This is useful for when you want to bias search results for a chunk. The magnitude only matters relative to other chunks in the chunk's dataset dataset.
 * @member {Number} weight
 */
CreateChunkData.prototype['weight'] = undefined;


CreateChunkData.OneOf = ["ChunkData", "CreateBatchChunkData"];

export default CreateChunkData;

